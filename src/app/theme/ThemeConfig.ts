import type { ThemeConfig } from 'antd';

const theme: ThemeConfig = {
  token: {
    fontSize: 16,
    colorPrimary: 'rgb(14 165 233 / var(--tw-bg-opacity, 1))', // Customize your primary color here
    colorBgContainer:'white'
  },
};

export default theme;